/**
 * Enum of permissions
 * @type {{SAY: number, MSG: number, WELCOME: number, GRANT: number, REVOKE: number}}
 */
const PERMISSIONS = {
    'SAY': 1,
    'MSG': 2,
    'WELCOME': 3,
    'GRANT': 4,
    'REVOKE': 5,
    'WARN': 6,
    'PERMISSIONS': 7,
};

module.exports = PERMISSIONS;